{
  "name": "@portfolio/ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "lint": "python -m ruff check app/",
    "test": "python -m pytest tests/ -v --asyncio-mode=auto",
    "test:coverage": "python -m pytest tests/ -v --asyncio-mode=auto --cov=app --cov-report=term-missing"
  }
}
